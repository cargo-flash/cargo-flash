'use client'

// Confetti disabled for professional appearance
// This component is kept as a stub to maintain functionality
// and can be re-enabled in the future if needed

export function ConfettiCelebration() {
    // Professional tracking pages don't use confetti animations
    // Returning null to disable visual effect while keeping component interface
    return null
}
